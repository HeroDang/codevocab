<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginHorizontal="16dp"
    android:layout_marginVertical="8dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?android:attr/selectableItemBackground"
    app:cardBackgroundColor="#FFFFFF"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    app:strokeWidth="0dp">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Icon Container -->
        <com.google.android.material.card.MaterialCardView
            android:id="@+id/flIcon"
            android:layout_width="48dp"
            android:layout_height="48dp"
            app:cardBackgroundColor="#EDE7F6"
            app:cardCornerRadius="12dp"
            app:cardElevation="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <ImageView
                android:id="@+id/imgModuleIcon"
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_book1"
                app:tint="#5E35B1" />
        </com.google.android.material.card.MaterialCardView>

        <!-- Module Name -->
        <TextView
            android:id="@+id/tvModuleName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:ellipsize="end"
            android:maxLines="1"
            android:text="Business English"
            android:textColor="#212121"
            android:textSize="16sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toStartOf="@+id/ivShareContainer"
            app:layout_constraintStart_toEndOf="@+id/flIcon"
            app:layout_constraintTop_toTopOf="@+id/flIcon" />

        <!-- Word Count Row -->
        <LinearLayout
            android:id="@+id/llWordCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            app:layout_constraintBottom_toBottomOf="@+id/flIcon"
            app:layout_constraintStart_toStartOf="@+id/tvModuleName">

            <ImageView
                android:layout_width="14dp"
                android:layout_height="14dp"
                android:src="@drawable/ic_layers"
                app:tint="#757575" />

            <TextView
                android:id="@+id/tvWordCount"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:text="24 words"
                android:textColor="#757575"
                android:textSize="14sp" />
        </LinearLayout>

        <!-- Share Icon -->
        <FrameLayout
            android:id="@+id/ivShareContainer"
            android:layout_width="32dp"
            android:layout_height="32dp"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@+id/flIcon">
            
             <ImageView
                android:id="@+id/ivShare"
                android:layout_width="17dp"
                android:layout_height="14dp"
                android:layout_gravity="center"
                android:src="@drawable/ic_menu_point"
                app:tint="#757575" />
        </FrameLayout>

        <!-- Shared Info (Visible only in Shared Tab) -->
        <TextView
            android:id="@+id/tvSharedBy"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="By: QuocBao102"
            android:textColor="#757575"
            android:textSize="12sp"
            android:visibility="gone"
            app:layout_constraintStart_toStartOf="@id/tvModuleName"
            app:layout_constraintTop_toBottomOf="@+id/llWordCount"
            tools:visibility="visible" />

        <!-- Shared Action Buttons (Pending/Accept) -->
        <LinearLayout
            android:id="@+id/llSharedActions"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:orientation="vertical"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="@id/tvModuleName"
            app:layout_constraintTop_toBottomOf="@+id/tvSharedBy"
            tools:visibility="visible">

            <!-- Tag Pending -->
            <TextView
                android:id="@+id/btnPending"
                android:layout_width="match_parent"
                android:layout_height="36dp"
                android:background="@drawable/bg_status_pending"
                android:gravity="center"
                android:text="Pending..."
                android:textColor="#757575"
                android:textSize="14sp" />

            <!-- Accept Button for Pending State (Outlined Style) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnPendingAccept"
                style="@style/Widget.MaterialComponents.Button.OutlinedButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="Accept Request"
                android:textAllCaps="false"
                android:textColor="@color/blue_600"
                app:cornerRadius="8dp"
                app:icon="@drawable/ic_check"
                app:iconTint="@color/blue_600"
                app:strokeColor="@color/blue_200"
                app:strokeWidth="1dp" />

            <!-- Original Accept Button (Used for Accepted State or other logic) -->
            <com.google.android.material.button.MaterialButton
                android:id="@+id/btnAccept"
                style="@style/Widget.MaterialComponents.Button.UnelevatedButton"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:backgroundTint="#4CAF50"
                android:text="Accept"
                android:textAllCaps="false"
                app:cornerRadius="8dp"
                app:icon="@drawable/ic_check" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</com.google.android.material.card.MaterialCardView>