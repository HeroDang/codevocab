<?xml version="1.0" encoding="utf-8"?>
<resources>

<!--    <style name="Theme.CodeVocab" parent="android:Theme.Material.Light.NoActionBar" />-->
    <!-- Base application theme -->
    <style name="Theme.CodeVocab" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your colors -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorOnPrimary">@color/colorOnPrimary</item>
        <item name="colorSurface">@color/colorSurface</item>
        <item name="colorOnSurface">@color/colorOnSurface</item>
        <item name="colorOnBackground">@color/colorBackground</item>


<!--        <item name="android:fontFamily">@font/poppins</item>-->
        <!-- Nếu muốn toàn màn hình (Edge-to-Edge) -->
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/black</item>
    </style>

    <style name="CircleImageView">
        <item name="cornerSize">50%</item>
    </style>
</resources>